name: Publish Desktop

on:
  workflow_run:
    workflows:
      - Deploy Web
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref or SHA to publish
        required: false
        default: main
        type: string

concurrency:
  group: publish-desktop-${{ github.event.workflow_run.head_branch || github.ref_name }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  prepare:
    name: Prepare
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    outputs:
      checkout_ref: ${{ steps.detect.outputs.checkout_ref }}
      published_at: ${{ steps.detect.outputs.published_at }}
      should_publish: ${{ steps.detect.outputs.should_publish }}
    env:
      TARGET_REF: ${{ github.event.workflow_run.head_sha || inputs.ref || github.sha }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ env.TARGET_REF }}
          fetch-depth: 0
      - name: Detect desktop-related changes
        id: detect
        uses: actions/github-script@v8
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const currentSha = process.env.TARGET_REF;
            const publishedAt = new Date().toISOString();
            const desktopPatterns = [
              /^framezoo-desktop\//,
              /^package\.json$/,
              /^pnpm-lock\.yaml$/,
              /^pnpm-workspace\.yaml$/,
            ];

            core.setOutput("checkout_ref", currentSha);
            core.setOutput("published_at", publishedAt);

            if (context.eventName === "workflow_dispatch") {
              core.setOutput("should_publish", "true");
              await core.summary
                .addHeading("Desktop publish")
                .addRaw(`Manual publish for \`${currentSha}\`.`)
                .write();
              return;
            }

            const currentRun = context.payload.workflow_run;
            const runs = await github.paginate(github.rest.actions.listWorkflowRuns, {
              owner,
              repo,
              workflow_id: "deploy-web.yml",
              branch: currentRun.head_branch,
              per_page: 100,
            });
            const previousRun = runs
              .filter(
                (run) =>
                  run.id !== currentRun.id &&
                  run.conclusion === "success" &&
                  new Date(run.created_at) < new Date(currentRun.created_at),
              )
              .sort(
                (first, second) =>
                  new Date(second.created_at).getTime() -
                  new Date(first.created_at).getTime(),
              )[0];

            let baseSha = previousRun?.head_sha ?? null;
            if (!baseSha) {
              const commit = await github.rest.repos.getCommit({
                owner,
                repo,
                ref: currentSha,
              });
              baseSha = commit.data.parents?.[0]?.sha ?? null;
            }

            if (!baseSha) {
              core.setOutput("should_publish", "false");
              await core.summary
                .addHeading("Desktop publish")
                .addRaw(
                  "No comparable parent commit found. Skipping automatic desktop publish; use workflow_dispatch for an initial or manual release.",
                )
                .write();
              return;
            }

            const compare = await github.rest.repos.compareCommitsWithBasehead({
              owner,
              repo,
              basehead: `${baseSha}...${currentSha}`,
            });
            const changedFiles = (compare.data.files ?? []).map((file) => file.filename);
            const shouldPublish = changedFiles.some((file) =>
              desktopPatterns.some((pattern) => pattern.test(file)),
            );

            core.setOutput("should_publish", shouldPublish ? "true" : "false");
            await core.summary
              .addHeading("Desktop publish diff")
              .addRaw(`Base: \`${baseSha}\``, true)
              .addRaw(`Head: \`${currentSha}\``, true)
              .addRaw(
                shouldPublish
                  ? "Desktop-related changes detected."
                  : "No desktop or shared package changes detected.",
                true,
              )
              .addList(changedFiles.length > 0 ? changedFiles : ["No files returned by compare API."])
              .write();
      - name: Skip marker
        if: steps.detect.outputs.should_publish != 'true'
        run: echo "No desktop-related changes. Skipping publish."

  build-macos:
    name: Build macOS (${{ matrix.variant }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    needs: prepare
    if: needs.prepare.outputs.should_publish == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15-intel
            variant: mac-x64
            arch: x64
            command: pnpm --filter Framezoo package:release:mac:x64
          - runner: macos-15
            variant: mac-arm64
            arch: arm64
            command: pnpm --filter Framezoo package:release:mac:arm64
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
      - name: Check macOS signing secrets
        shell: bash
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
        run: |
          set -eu
          if [ -z "${CSC_LINK:-}" ]; then
            echo "::warning::Thiếu macOS signing secrets (CSC_LINK). Ứng dụng sẽ KHÔNG được code sign và tính năng OTA Update trên macOS sẽ KHÔNG hoạt động."
          else
            echo "macOS signing secrets có sẵn."
          fi
      - run: corepack enable
      - run: corepack prepare pnpm@11.9.0 --activate
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Set up Python 3.12 for the torrent engine sidecar
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Write env file
        shell: bash
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
        run: |
          set -eu
          if [ -z "${ENV_FILE:-}" ]; then
            echo "ENV_FILE secret is empty."
            exit 1
          fi
          printf '%s' "$ENV_FILE" > .env
          printf '%s' "$ENV_FILE" > framezoo-fe/.env

          BACKEND_URL=$(grep -Eo '^VITE_BACKEND_URL=(.*)' .env | cut -d= -f2- | tr -d '"' | tr -d "'" || echo "")
          if [ -n "$BACKEND_URL" ]; then
            echo "VITE_BACKEND_URL=$BACKEND_URL" >> "$GITHUB_ENV"
          fi

          DEVTOOLS=$(grep -Eo '^VITE_ENABLE_DEVTOOLS_PROTECTION=(.*)' .env | cut -d= -f2- | tr -d '"' | tr -d "'" || echo "")
          if [ -n "$DEVTOOLS" ]; then
            echo "VITE_ENABLE_DEVTOOLS_PROTECTION=$DEVTOOLS" >> "$GITHUB_ENV"
          fi
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Setup macOS native toolchain & libmpv
        run: |
          brew install mpv cmake
          echo "LIBMPV_ROOT=$(brew --prefix mpv)" >> "$GITHUB_ENV"
          echo "CMAKE=$(brew --prefix)/bin/cmake" >> "$GITHUB_ENV"
      - name: Build desktop artifacts
        shell: bash
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          if [ -z "$CSC_LINK" ]; then unset CSC_LINK; fi
          if [ -z "$CSC_KEY_PASSWORD" ]; then unset CSC_KEY_PASSWORD; fi
          if [ -z "$APPLE_ID" ]; then unset APPLE_ID; fi
          if [ -z "$APPLE_APP_SPECIFIC_PASSWORD" ]; then unset APPLE_APP_SPECIFIC_PASSWORD; fi
          if [ -z "$APPLE_TEAM_ID" ]; then unset APPLE_TEAM_ID; fi
          ${{ matrix.command }}
      - name: Upload desktop artifacts
        uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.variant }}
          if-no-files-found: error
          path: |
            framezoo-desktop/release/latest-mac.yml
            framezoo-desktop/release/Framezoo-*-${{ matrix.arch }}-mac.dmg
            framezoo-desktop/release/Framezoo-*-${{ matrix.arch }}-mac.zip
            framezoo-desktop/release/Framezoo-*-${{ matrix.arch }}-mac.zip.blockmap

  build-windows:
    name: Build Windows (${{ matrix.variant }})
    runs-on: windows-latest
    timeout-minutes: 90
    needs: prepare
    if: needs.prepare.outputs.should_publish == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: win-x64
            command: pnpm --filter Framezoo package:release:win:x64
            arch: x64
          - variant: win-arm64
            command: pnpm --filter Framezoo package:release:win:arm64
            arch: arm64
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
      - run: corepack enable
      - run: corepack prepare pnpm@11.9.0 --activate
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - name: Set up Python 3.12 for the torrent engine sidecar
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Node headers for native addon
        shell: pwsh
        run: |
          $nodeVersion = node -p "process.versions.node"
          $nodeGypDir = Join-Path $env:RUNNER_TEMP "node-gyp"

          pnpm exec node-gyp install `
            --target $nodeVersion `
            --arch=${{ matrix.arch }} `
            --devdir $nodeGypDir

          $includeDir = Join-Path $nodeGypDir "$nodeVersion\include\node"
          $nodeLib = Join-Path $nodeGypDir "$nodeVersion\${{ matrix.arch }}\node.lib"

          if (!(Test-Path (Join-Path $includeDir "node_api.h"))) {
            throw "Node headers not found: $includeDir"
          }
          if (!(Test-Path $nodeLib)) {
            throw "Node library not found: $nodeLib"
          }

          "NODE_INCLUDE_DIR=$includeDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "NODE_LIB=$nodeLib" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
      - name: Write env file
        shell: bash
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
        run: |
          set -eu
          if [ -z "${ENV_FILE:-}" ]; then
            echo "ENV_FILE secret is empty."
            exit 1
          fi
          printf '%s' "$ENV_FILE" > .env
          printf '%s' "$ENV_FILE" > framezoo-fe/.env

          BACKEND_URL=$(grep -Eo '^VITE_BACKEND_URL=(.*)' .env | cut -d= -f2- | tr -d '"' | tr -d "'" || echo "")
          if [ -n "$BACKEND_URL" ]; then
            echo "VITE_BACKEND_URL=$BACKEND_URL" >> "$GITHUB_ENV"
          fi

          DEVTOOLS=$(grep -Eo '^VITE_ENABLE_DEVTOOLS_PROTECTION=(.*)' .env | cut -d= -f2- | tr -d '"' | tr -d "'" || echo "")
          if [ -n "$DEVTOOLS" ]; then
            echo "VITE_ENABLE_DEVTOOLS_PROTECTION=$DEVTOOLS" >> "$GITHUB_ENV"
          fi
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Cache Windows libmpv SDK
        uses: actions/cache@v4
        with:
          path: ./win-mpv-dev-${{ matrix.variant }}
          # Keyed on the fetch script so SDK upgrades invalidate the cache,
          # and on the lockfile so dependency changes never reuse stale SDK.
          key: mpv-sdk-${{ matrix.variant }}-${{ hashFiles('framezoo-desktop/scripts/fetch-windows-libmpv.mjs') }}
          restore-keys: |
            mpv-sdk-${{ matrix.variant }}-
      - name: Setup Windows libmpv SDK
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node framezoo-desktop/scripts/fetch-windows-libmpv.mjs --target ${{ matrix.variant == 'win-x64' && 'win32-x64' || 'win32-arm64' }} --out ./win-mpv-dev-${{ matrix.variant }}
      - name: Build desktop artifacts
        shell: bash
        run: ${{ matrix.command }}
      # Strongest gate: load the addon exactly as the packaged app would, from
      # the win-unpacked output, inside real Electron. A broken or missing
      # native binary fails the job and the release is never published.
      #
      # NOTE: `pnpm --filter Framezoo exec` switches the working directory into
      # the framezoo-desktop package, so the paths below are package-relative.
      - name: Verify packaged native libmpv (Electron)
        shell: bash
        run: |
          set -eu
          pnpm --filter Framezoo exec electron scripts/verify-libmpv-native.mjs \
            --addon "release/win-unpacked/resources/native/libmpv.node" \
            --runtime-dir "release/win-unpacked/resources/libmpv"
      - name: Upload desktop artifacts
        uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.variant }}
          if-no-files-found: error
          path: |
            framezoo-desktop/release/latest.yml
            framezoo-desktop/release/Framezoo-*-${{ matrix.arch }}.exe
            framezoo-desktop/release/Framezoo-*-${{ matrix.arch }}.exe.blockmap

  publish:
    name: Publish
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs:
      - prepare
      - build-macos
      - build-windows
    if: needs.prepare.outputs.should_publish == 'true'
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ needs.prepare.outputs.checkout_ref }}
      - run: corepack enable
      - run: corepack prepare pnpm@11.9.0 --activate
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Download packaged artifacts
        uses: actions/download-artifact@v5
        with:
          path: desktop-artifacts
      - name: Build release bundle
        shell: bash
        run: |
          set -eu
          node scripts/create-desktop-release-bundle.js \
            --input-root desktop-artifacts \
            --output-dir desktop-release-bundle \
            --channel stable \
            --published-at "${{ needs.prepare.outputs.published_at }}"
          node scripts/verify-desktop-release-bundle.js desktop-release-bundle
          tar -czf desktop-release-bundle.tar.gz -C desktop-release-bundle .
      - name: Install SSH tools
        run: sudo apt-get update && sudo apt-get install -y openssh-client
      - name: Start SSH agent
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          eval "$(ssh-agent -s)"
          echo "$VPS_SSH_KEY" | tr -d '\r' | ssh-add -
          echo "SSH_AUTH_SOCK=$SSH_AUTH_SOCK" >> "$GITHUB_ENV"
          echo "SSH_AGENT_PID=$SSH_AGENT_PID" >> "$GITHUB_ENV"
      - name: Trust VPS host
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_PORT: ${{ secrets.VPS_PORT }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          ssh-keyscan -p "${VPS_PORT:-22}" "$VPS_HOST" >> ~/.ssh/known_hosts
      - name: Upload bundle and publish on VPS
        shell: bash
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_PORT: ${{ secrets.VPS_PORT }}
          VPS_USER: ${{ secrets.VPS_USER }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          DEPLOY_PROJECT_NAME: ${{ secrets.DEPLOY_PROJECT_NAME }}
        run: |
          set -eu

          if [ -z "${ENV_FILE:-}" ]; then
            echo "ENV_FILE secret is empty."
            exit 1
          fi

          DEPLOY_USER="${VPS_USER:-betakiot}"
          DEPLOY_TARGET="${DEPLOY_USER}@${VPS_HOST}"
          DEPLOY_PATH="${DEPLOY_PATH:-/home/framezoo}"
          DEPLOY_PROJECT_NAME="${DEPLOY_PROJECT_NAME:-framezoo}"
          STAGING_PATH="${DEPLOY_PATH}/desktop-publish-staging"

          ssh -p "${VPS_PORT:-22}" "${DEPLOY_TARGET}" "mkdir -p '${STAGING_PATH}'"
          scp -P "${VPS_PORT:-22}" desktop-release-bundle.tar.gz "${DEPLOY_TARGET}:${STAGING_PATH}/desktop-release-bundle.tar.gz"

          printf '%s' "$ENV_FILE" | ssh -p "${VPS_PORT:-22}" "${DEPLOY_TARGET}" \
            "set -eu; ENV_TMP=\$(mktemp /tmp/framezoo_env.XXXXXX); trap 'rm -f \"\$ENV_TMP\"' EXIT; cat > \"\$ENV_TMP\"; chmod 600 \"\$ENV_TMP\"; cd '${DEPLOY_PATH}'; PROJECT_NAME='${DEPLOY_PROJECT_NAME}' COMPOSE_FILE='docker-compose.prod.yml' ENV_FILE_PATH=\"\$ENV_TMP\" ./scripts/deploy.sh publish-desktop"
